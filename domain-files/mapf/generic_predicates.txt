
% object(ObjectName,ObjectType).

% object(track(1025),track).
% object(agent(1),agent).
% object(train(1),train).

% node((Row,Column)).
%   A declared position.

% node((0,0)).
% node((0,7)).
% node((0,10)).

% position(Node,Role,ObjectName).

%  domain placement
% position((0,0),start,agent(1)).
% position((0,7),start,train(1)).
% position((0,10),cell,track(1025)).

% property(ObjectName,PropertyName,PropertyValue).

% property(train(1),dep,5).
% property(train(1),arr,10).
% property(train(1),direction,east).
% property(train(1),speed,1).

% image(ObjectName,Role,ImagePath).

% image(train(1),start,"img/train1.png").
% image(track(1025),cell,"img/straight_0.png").

